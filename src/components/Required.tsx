import React from 'react';

import Icon from 'src/components/Icon';

function Required() {
  return <Icon name="asterisk" size={8} style={{ color: 'red', marginLeft: 5, marginTop: -4 }} />;
}

export default Required;
